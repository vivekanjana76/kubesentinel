apiVersion: v1
kind: Service
metadata:
  name: sacrificial
  namespace: kubesentinel
  labels:
    app: sacrificial
    app.kubernetes.io/name: sacrificial
    app.kubernetes.io/component: target-app
    app.kubernetes.io/part-of: kubesentinel
spec:
  type: ClusterIP
  selector:
    app: sacrificial
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
